# 開發過程中發現的範圍外功能缺口

> 任何角色（Designer/Design Reviewer/Coder/Reviewer/Tester/Fixer）在分析或開發過程中，若發現明確需要另開 feature 處理、但不阻塞目前任務的範圍外問題，append 一行到這裡（不要自行擴大目前 feature 的 scope、不要動手做、不要自己呼叫 `4x new`——這是給使用者決定優先度的候選清單）。
>
> 格式：`- YYYY-MM-DD [來源 FXXX] 發現內容 — 建議：<新 feature 名稱/範圍>`
>
> 已經開了對應 feature 的項目，在後面加 `[已開 FYYY]` 並保留紀錄（不刪除，方便追溯發現脈絡）。

- 2026-07-07 [來源 F132] `internal/orchestrator/resume.go` 的 `SmartResumePhase`（crash resume 用）在 deep-review PASS 後一律直接轉 `PhaseAccepting`，完全跳過 Fixing；而正常 live-run 路徑（`deepTransitionAccepting`）在 profile 啟用 Fixing 時，deep-review 為 CONDITIONAL PASS（有 warning）會進 Fixing。兩條路徑對「同一個 deep-review-report.md」可能推導出不同的下一個 phase，是 resume 與 live-run 行為不一致的既有缺口（F132 之前就存在，此次只是发现，未修改該路徑，維持既有行為）。— 建議：新開 feature 讓 `SmartResumePhase` 對 deep-review PASS 的判斷與 `deepTransitionAccepting`（含乾淨 PASS 判斷）對齊。 [已開 F133]
- 2026-07-07 [來源 F132] `internal/orchestrator/phase.go` 的 `NextPhaseAfter` 內 `case protocol.PhaseDeepReviewing` 分支，在目前 live-run 主迴圈中因 `routePhase` 已將 `PhaseDeepReviewing` 導向專屬的 `runDeepReviewPhase`（見 orchestrator.go `routePhase`），實際上是永遠不會被觸發的死碼，且其邏輯（一律轉 Fixing、不判斷乾淨 PASS）與 `deepTransitionAccepting` 的新行為不一致。— 建議：新開 feature 清理此死碼或補上測試以確認其用途（若真的無用途則移除，若有隱藏呼叫路徑則需對齊行為）。 [已直接修正：dead code 已刪除]
- 2026-07-07 [來源 F132] Review round 1 期間發現：`rounds/round-{n}/role-learnings.json` 是同一個 round 目錄下所有角色（coder/reviewer/tester/fixer/design-reviewer/deep-reviewer）共用同一檔名（見 `internal/protocol/workspace.go` `RoleLearningsFileName` 與各 `templates/*.tmpl`），且 `RoleLearningsFile` 結構只存一個 `role` 欄位。同一 round 內若有多個角色都寫了 role-learnings.json，後寫入者會直接覆蓋前者，`harvestRoleLearnings`（`internal/prompt/learnings.go`）每個 round 目錄只解析一份檔案，較早角色的 learnings 會在未被記錄的情況下靜默遺失（本輪即實測發生：coder 的 role-learnings.json 被 reviewer 覆寫前已先讀出保留，但若非人工注意會遺失）。— 建議：新開 feature 讓每個角色寫入獨立檔名（如 `role-learnings-{role}.json`）或改為 append 制，避免 harvest 階段的 learnings 遺失。 [已開 F134]
- 2026-07-07 [來源 F138] `.github/workflows/release.yml` 的 macos job 仍執行 `make package-macos`（ad-hoc 簽名），未接上 F138 新增的 Developer ID 簽名與公證流程；正式 GitHub Release 產出的 .dmg 仍未經公證。要在 CI 自動簽名+公證需設定 GitHub Secrets（Developer ID 憑證、App Store Connect API key）並在 workflow 匯出為對應環境變數後改呼叫 `make dashboard-release`。— 建議：新開 feature 讓 CI release 產出已公證的 .dmg。 [已開 F139]
- 2026-07-08 [source: model-routing] roles 白名單（settings 驗證）不含 fixer 與 mini-coder，無法對這兩個真實 role 做 per-role model 路由（fixer 應可設 opus、mini-coder 應可設 sonnet）；昨日 kairos 856404a 加 fixer entry 後 settings 載入失敗即此因 — suggested follow-up: 擴充 roles 白名單納入 fixer/mini-coder，並順手檢查 roleCategoryMap 是否同步 [已開 F145]
- 2026-07-08 [來源 F142] parallel_review_test 模式下 tester 與 reviewer 同時啟動，但 state.json 只記錄 phase=reviewing / role=reviewer；tester agent 讀 state.json 發現不是自己的 phase 會拒絕執行（self-protection），導致 tester 無產出觸發 amending。修法方向：在 tester prompt 加 parallel 模式說明，或在 state.json 加 parallel 標記讓 agent 識別 — 建議：parallel-review-test state 信號對齊 [已開 F151]
- 2026-07-08 [source: F144-conditional-pass-same-round] parallel review-test 模式（`ParallelReviewTest==true`，走 `RunReviewTestParallel`）下 reviewer 的 CONDITIONAL PASS 不走 F144 的同輪收斂路徑，warning 仍會直接放行到後續 phase；本輪刻意不接線（out of scope）。— suggested follow-up: 若量測顯示該模式使用率高，另開 feature 讓 parallel 路徑也做同輪 CONDITIONAL PASS 收斂 [已開 F151]
- 2026-07-08 [source: F146-recovery-respect-manual-phase] designer 階段手動跑 `4x check` 時，guard 把 orchestrator 於 run-start 自設的 status: not-started → in-progress 誤判為「designer modified non-repos fields」— suggested follow-up: designer guard 應忽略 orchestrator 自身的 status flip（或以 run baseline 而非 git HEAD 比對 feature YAML） [已開 F152]
- 2026-07-08 [source: F150-profile-aware-role-prompts] `.4x/plugins/*.md`（含 CLAUDE.md/GEMINI.md/cursorrules/codex-AGENTS.md 等）的角色表寫「Acceptor Writes: acceptor-report.md」，但實際 templates/acceptor.md.tmpl 明訂只可寫 final-report.md 與 retro-learnings.json，無 acceptor-report.md；此為過時角色表文件債 — suggested follow-up: 校正所有 plugin 角色表使其與 runtime（templates + guard）一致 [已開 F155]
- 2026-07-09 [source: F151 post-merge review] tester.md.tmpl 的 parallel 豁免依賴 parallelReview marker，但 marker 寫入鏈是 best-effort（WriteState 失敗只 log、SyncFeatureToWorktree 複製失敗只 warn），marker 缺席時 tester 對 phase/role 錯位失去豁免依據會拒跑 — suggested follow-up: parallel marker 寫入失敗改為 fail-fast 或 tester prompt 提供 fallback 判斷
- 2026-07-09 [source: F156-egps-executable-ac] main 分支已追蹤 ~2787 個 .cache/golangci-lint/ 工具 cache 檔（早於本 feature，疑似 F151 帶入）— suggested follow-up: 清理 repo 既有 cache 污染並在 main 補 .cache/ gitignore
- 2026-07-09 [source: F161-dashboard-auth] `4x retry` 不重置 state.json 的 consecutiveNoProgress，殘留值=3 時 retry 後任何 phase 一啟動就被 no-progress 偵測秒殺打回 needs-attention，須手動改 state.json 歸零 — suggested follow-up: retry 命令應重置 consecutiveNoProgress/stopReason
- 2026-07-09 [source: F162-verify-command-allowlist] codex acceptor（gpt-5.5）讀取 acceptor prompt（含 review-package.md 137.7K）後 5s 內 exit-1 且零輸出，兩次重試皆同；review/verify/build-gate 全綠，純 acceptor 收尾步驟被 codex 卡住。保留 feature 待 claude 額度恢復後用 claude acceptor 收尾 — suggested follow-up: acceptor prompt 對超大 review-package 應分段或截斷；或偵測 runner context 上限自動降級
- 2026-07-09 [source: F163-runner-env-hardening] `cmd/4x/check_path_test.go`/`guard_tool_write_test.go` 的 `run4xIO` 以 `cmd.Env = append(os.Environ(), extraEnv...)` 全量繼承目前 process 環境，spawn 真實 4x 二進位測 role/repo-scope deny；當測試在 4x runner 子程序（本身帶 `FOURX_FEATURE_ID`/`FOURX_BIN`）內執行時，子程序誤讀真實 `.4x/run/{現行 feature}/state.json` 而非 fixture 狀態，`EvaluateWritePathForRun` fail-open 放行，使 `TestCheckPath_RoleScopeDeny`/`TestCheckPath_RepoScope`/`TestGuardToolWrite_EditIntercept` 的 deny 斷言誤判為 FAIL（在 main 分支同樣可重現，非 F163 引入；清除該兩變數後兩分支皆綠）— suggested follow-up: `run4xIO` 改用白名單環境（不繼承 `FOURX_*`）或顯式清除，避免未來 4x 自身 dogfood session 跑 make test 時重現此假陽性
- 2026-07-09 [source: F165] 複核 F163 已記錄的 run4xIO 假陽性：TestCheckPath_RoleScopeDeny / TestCheckPath_RepoScope / TestGuardToolWrite_EditIntercept（共 6 子測試）在 main HEAD dd866e97 於「4x runner 子程序內跑 make test」時假紅——根因是 run4xIO 全量繼承 FOURX_FEATURE_ID/FOURX_BIN，spawn 的 4x 讀到現行 dogfooding feature 的 state 而非 fixture，deny 分支不觸發；`env -u FOURX_FEATURE_ID -u FOURX_BIN` 後 6 者全綠，非 production bug。F165 已將「run4xIO 環境隔離（剝除 FOURX_*）」納入 scope 修復（見 F165 task-brief 設計裁決），此 gap 由 F165 一併解決
- 2026-07-09 [source: F165] F165 已修 run4xIO 剝除 FOURX_*，但 cli_test.go 的 run4x helper 未設 cmd.Env（完全繼承父環境含 FOURX_*），目前不被 scope-sensitive 測試使用故無假陽性；若未來有 feature-resolving 命令測試改用 run4x，同類污染會重現 — suggested follow-up: 將 run4x 也統一走剝除 FOURX_* 的環境，或抽共用 spawn helper
- 2026-07-10 [source: F168-codex-usage-observability autopilot recovery] `4x retry` 的 `--to` 預設值硬編碼為 `accepting`（見 cmd/4x/retry.go newRetryCmd），未依 state.json 進入 blocked/needs-attention 前保留的 `role` 欄位反推正確的目標 phase；F168 designer round0 因 claude session 額度撞牆 soft-fail 進 blocked（role=designer），不加 `--to designing` 直接 `4x retry` 會跳去 accepting，Coder 從未執行、程式碼零異動下 acceptor 直接判 needs-attention，浪費一次呼叫（$0.83）— suggested follow-up: retryTransition 預設 target 改用 `state.RoleToPhase(cur.Role)`（若存在對應關係）取代寫死的 PhaseAccepting，僅在無法反推時才 fallback accepting；並在 --to 缺席時印出訊息告知使用者採用了哪個自動偵測的 phase [已開 F169]
- 2026-07-10 [source: F170-worktree-path-fail-open-hardening] path-containment（解 symlink 後判定 target 是否落在 root 內）語意在 codebase 已有 3 份近似實作：cmd/4x/check.go 的 pathWithinRoot、internal/server/screenshots.go:156-169、internal/protocol/design_doc.go:50-61；安全敏感判定散成三處易在邊界（尾斜線/`.`/根路徑）語意悄悄分歧 — suggested follow-up: 收斂為單一 internal/pathutil.WithinRoot(root, target)（內含 realpathBestEffort 的 best-effort 祖先解析，涵蓋尚未建立的寫入目標），讓 server/protocol/cmd 共用同一份 containment 語意
