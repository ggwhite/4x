[package]
name = "4x-live"
version = "0.1.0"
edition = "2021"
description = "4x Live desktop shell (Windows/Linux)"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-shell = "2"

[features]
custom-protocol = ["tauri/custom-protocol"]
